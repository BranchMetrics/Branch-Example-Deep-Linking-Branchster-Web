---
machine:
  node:
    version: 0.10.31

dependencies:
  cache_directories:
    - "~/nvm/v0.10.31"
  post:
    - npm install && npm install -g grunt grunt-cli bower
    - bower install

test:
  override:
    - grunt

